type Choice @model 
  @key(name: "votesByDate", fields: ["type", "createdAt"], queryField: "votesByDate")
  {
  id: ID!
  name: String!
  animal: String!
  description: String
  createdAt: String!
  type: String!
}